extends base
include _authShowcase

block content
  main.main.auth-page
    section.auth-layout
      .auth-panel
        .auth-panel__inner
          .auth-brand
            a.auth-brand__logo(href='/')
              img(src='/img/logo-blue.png' alt='LLK Travel logo')
            .auth-brand__text
              span LLK Travel
              small LƯU GIỮU - LẮNG ĐỌNG - KHÁM PHÁ
          h1.auth-panel__title Khởi tạo tài khoản nội bộ
          p.auth-panel__subtitle Tạo tài khoản để đồng bộ điều hành, hướng dẫn viên và bộ phận chăm sóc khách hàng trong cùng một nền tảng.
          form.form.form--signup
            .auth-field
              label.auth-field__label(for='name') Họ và tên
              .auth-field__input
                i.fa-regular.fa-user
                input#name(type='text', placeholder='Nguyễn Minh Châu', required)
            .auth-field
              label.auth-field__label(for='email') Email công việc
              .auth-field__input
                i.fa-regular.fa-envelope
                input#email(type='email', placeholder='ban@llk.travel', required)
            .auth-field
              label.auth-field__label(for='password') Mật khẩu
              .auth-field__input
                i.fa-solid.fa-lock
                input#password(type='password', placeholder='Ít nhất 8 ký tự', required, minlength='8')
            .auth-field
              label.auth-field__label(for='password-confirm') Xác nhận mật khẩu
              .auth-field__input
                i.fa-solid.fa-shield-halved
                input#password-confirm(type='password', placeholder='Nhập lại mật khẩu', required, minlength='8')
            button.btn.auth-btn(type='submit') Tạo tài khoản
          .auth-meta
            span Đã có tài khoản?
            a.auth-link(href='/login') Đăng nhập
      +authShowcase()
